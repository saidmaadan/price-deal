# import * from './generated/prisma.graphql'
type Mutation{
    createProduct(
        title: String, 
        description: String,
        image: String,
        largeImage: String,
        originalPrice: Int
        discount: Float
        price: Int
        ): Product!
}

type Query{
    products: [Product]!
}